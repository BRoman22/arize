export type TInvestCard = {
  id: number;
  name: string;
  titleImage: string;
  rate: string;
  subtitle: string;
  backImage: string;
};

export type TSuccesCard = {
  id: number;
  name: string;
  titleImage: string;
  rate: string;
  subtitle: string;
};

export type TTeamCard = {
  name: string;
  position: string;
  photo: string;
};

export type TQuestions = {
  id: number;
  question: string;
  answer: string;
};
