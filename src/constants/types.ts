export type TInvestCard = {
  id: number;
  name: string;
  titleImage: string;
  rate: string;
  subtitle: string;
  backImage: string;
};

export type TSuccesCard = {
  id: number;
  name: string;
  titleImage: string;
  rate: string;
  subtitle: string;
};
